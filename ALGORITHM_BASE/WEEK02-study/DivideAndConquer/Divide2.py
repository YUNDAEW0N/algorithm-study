# 분할정복 2
# 합병 정렬 알고리즘

# 배열의 구간 l,r 을 정렬하는 알고리즘
# l=r이면 직접 해결
# 원소가 하나인 배열은 이미 정렬된 상태

# l<r 이면 더 작은 문제로 나눠서 해결
  # m을 l과 r의 중간 지점이라고 할때
  # l,m 과 m+1,r을 각각 정렬한다음 (분할)
  # 정렬된 두배열을 정렬된 하나의 배열로 합병  (정복)


